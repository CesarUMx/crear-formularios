---
import Layout from '../../layouts/Layout.astro';
import PublicExamSteps from '../../components/exams/PublicExamSteps';

export const prerender = false;

const { slug } = Astro.params;

if (!slug) {
  return Astro.redirect('/');
}
---

<Layout title="Examen Público">
  <PublicExamSteps client:load slug={slug} />
</Layout>
