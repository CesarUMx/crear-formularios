---
import AdminLayout from '../../../../layouts/AdminLayout.astro';
import ProtectedRoute from '../../../../components/auth/ProtectedRoute';
import ExamStatistics from '../../../../components/exams/ExamStatistics';

export const prerender = false;

const { id } = Astro.params;

if (!id) {
  return Astro.redirect('/admin/exams');
}
---

<AdminLayout title="Estadísticas del Examen" currentPath="/admin/exams">
  <ProtectedRoute client:load>
    <div class="max-w-7xl mx-auto">
      <!-- Header -->
      <div class="mb-6">
        <div class="flex items-center justify-between">
          <div>
            <h1 class="text-2xl font-bold text-gray-900">Estadísticas del Examen</h1>
            <p class="text-gray-600 mt-1">Análisis de resultados y rendimiento</p>
          </div>
          <div class="flex gap-2">
            <a
              href={`/admin/exams/${id}/attempts`}
              class="px-4 py-2 text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 transition"
            >
              👥 Ver Intentos
            </a>
            <a
              href={`/admin/exams/${id}`}
              class="px-4 py-2 text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 transition"
            >
              ✏️ Editar
            </a>
            <a
              href="/admin/exams"
              class="px-4 py-2 text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 transition"
            >
              ← Volver
            </a>
          </div>
        </div>
      </div>
      
      <ExamStatistics client:load examId={id} />
    </div>
  </ProtectedRoute>
</AdminLayout>
