---
import AdminLayout from '../../../../layouts/AdminLayout.astro';
import ProtectedRoute from '../../../../components/auth/ProtectedRoute';
import ExamAttemptsList from '../../../../components/exams/ExamAttemptsList';

export const prerender = false;

const { id } = Astro.params;

if (!id) {
  return Astro.redirect('/admin/exams');
}
---

<AdminLayout title="Intentos del Examen" currentPath="/admin/exams">
  <ProtectedRoute client:load>
    <div class="max-w-7xl mx-auto">
      <!-- Header -->
      <div class="mb-6">
        <div class="flex items-center justify-between">
          <div>
            <h1 class="text-2xl font-bold text-gray-900">Intentos del Examen</h1>
            <p class="text-gray-600 mt-1">Revisa y califica los intentos de los estudiantes</p>
          </div>
          <div class="flex gap-2">
            <a
              href={`/admin/exams/${id}/stats`}
              class="px-4 py-2 text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 transition"
            >
              📊 Estadísticas
            </a>
            <a
              href={`/admin/exams/${id}`}
              class="px-4 py-2 text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 transition"
            >
              ✏️ Editar
            </a>
            <a
              href="/admin/exams"
              class="px-4 py-2 text-gray-700 bg-white border border-gray-300 rounded-lg hover:bg-gray-50 transition"
            >
              ← Volver
            </a>
          </div>
        </div>
      </div>
      
      <ExamAttemptsList client:load examId={id} />
    </div>
  </ProtectedRoute>
</AdminLayout>
